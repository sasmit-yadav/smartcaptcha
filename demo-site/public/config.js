/**
 * SmartCaptcha Configuration
 * Centralized backend URL configuration - update this single file to change backend endpoint
 */

window.SMARTCAPTCHA_CONFIG = {
  // Backend API endpoint - update this for different environments
  BACKEND_URL: 'https://smartcaptcha-backend.onrender.com',
  
  // API key (for demo purposes)
  API_KEY: 'demo-key',
  
  // Debug mode
  DEBUG: false,
};
